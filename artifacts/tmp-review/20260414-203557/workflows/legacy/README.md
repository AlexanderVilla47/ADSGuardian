# Legacy workflows

This folder stores JSON backups of workflows removed from the n8n instance during controlled cleanup.

Rules:

- Do not deploy these files as active MVP flows.
- Use only for rollback or forensic diff.
- Any restore must be followed by explicit activation checks.
