# Frontend local build/deps
frontend/node_modules/
frontend/dist/

# Local temp/work artifacts
temp/
artifacts/local-cleanup-*/

# Ad-hoc local dumps (root)
execution_*.json
executions_*.json
exec_*.json
workflow.json
workflow_clean.json
workflow_data.json
workflows_list.json
meta_mock_workflow.json

# --- Agent tooling / config (not product) ---
.agents/
.atl/
.claude/
skills-lock.json

# --- Process / SDD artifacts ---
openspec/
test.json

# --- Review / cleanup temp artifacts ---
artifacts/

# --- Workflow backups, iterations & edits (keep only canonical) ---
workflows/backups/
workflows/non-canonical/
workflows/edits/
